# ── Production environment (used by Netlify build) ───────────────────────────
# Step 1: Deploy backend to Render.com using backend/render.yaml
# Step 2: Copy your Render service URL here (e.g. https://park-prabandh-backend.onrender.com)
# Step 3: Add BOTH vars below in Netlify → Site Settings → Environment Variables
VITE_API_URL=https://park-prabandh-backend.onrender.com

# Add your actual key only in Netlify env vars dashboard — do NOT commit real keys here
VITE_API_MARKET_KEY=replace_in_netlify_dashboard
